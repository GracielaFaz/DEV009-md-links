const mdLinks = require('../data.js');

describe('pathExists', () => {

    it('Should catch error when the path does not exist', () => {
      expect(pathExists()).toBe(typeof Promise);
    });
  
    it('Should reject the promise when the path does not exist', () => {
       
    })
  
  });